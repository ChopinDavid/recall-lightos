<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG-ONLY overlay. Enables cleartext HTTP so the emulator can reach the
     dev anki-bridge at http://10.0.2.2:8000. Release builds do NOT include
     this (Light's signed builds are release); real-phone deployments use TLS. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
